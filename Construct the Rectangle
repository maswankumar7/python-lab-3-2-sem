from typing import List
import math

class Solution:
    def constructRectangle(self, area: int) -> List[int]:
        width = int(math.isqrt(area))  # safer than area ** 0.5

        while area % width != 0:
            width -= 1

        return [area // width, width]
